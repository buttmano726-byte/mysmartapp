name: Build APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Get Buildozer Docker
        uses: ArtemGovorov/buildozer-action@v1
        with:
          command: buildozer android debug

